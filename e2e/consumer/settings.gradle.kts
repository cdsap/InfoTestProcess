pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

plugins {
    id("io.github.cdsap.testprocess") version "2.1.0"
}

rootProject.name = "info-test-process-e2e-consumer"
