pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

plugins {
    id("io.github.cdsap.testprocess") version "2.0.0"
}

rootProject.name = "info-test-process-e2e-consumer"
