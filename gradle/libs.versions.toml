[versions]
kotlin = "2.4.20"
develocity = "4.5.1"
serialization = "1.11.0"
junit = "4.13.2"
buildObservabilitySchema = "0.0.3"
jsonSchemaValidator = "3.0.7"

[libraries]
develocity-gradlePlugin = { module = "com.gradle:develocity-gradle-plugin", version.ref = "develocity" }
kotlinx-serializationJson = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "serialization" }
junit = { module = "junit:junit", version.ref = "junit" }
build-observability-schema = { module = "io.github.cdsap:build-observability-schema", version.ref = "buildObservabilitySchema" }
json-schema-validator = { module = "com.networknt:json-schema-validator", version.ref = "jsonSchemaValidator" }

[plugins]
pluginPublish = { id = "com.gradle.plugin-publish", version = "2.2.1" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
